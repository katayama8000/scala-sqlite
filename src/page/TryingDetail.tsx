import React, { FC } from 'react';
import { Button, View } from 'react-native';

export const TryingDetailScreen: FC = () => {
  return (
    <View>
      <Button title="detail" />
    </View>
  );
};
